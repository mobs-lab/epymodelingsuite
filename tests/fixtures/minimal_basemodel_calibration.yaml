# Minimal SIR model for calibration E2E testing
# Based on production config structure but simplified for fast execution
model:
  meta:
    name: "minimal_sir_calibration"
    version: "1.0.0"
    date: "2025-01-01"
    description: "Minimal SIR model for calibration E2E testing"

  timespan:
    start_date: "2025-01-01"
    end_date: "2025-03-01"
    delta_t: 1.0

  simulation:
    resample_frequency: W-SAT

  random_seed: 42

  population:
    # population name specified in modelset config
    age_groups:
      - "0-4"
      - "5-17"
      - "18-49"
      - "50-64"
      - "65+"

  compartments:
    - id: S
      label: "Susceptible"
      init: default
    - id: I
      label: "Infectious"
      init: calibrated  # Initial infections calibrated
    - id: R
      label: "Recovered"

  transitions:
    - type: mediated
      source: S
      target: I
      mediator: I
      rate: beta
    - type: spontaneous
      source: I
      target: R
      rate: mu

  parameters:
    beta:
      type: calibrated  # Calibrate transmission rate
    mu:
      type: scalar
      value: 0.1
